# Python Rules

- Target Python 3.11+.
- Use type hints for public functions.
- Prefer standard-library functionality where it keeps the assignment simple.
- Keep validation close to the Pydantic models.
- Keep SQL access in the service/database layer.
- Avoid unnecessary abstractions or frameworks.
- Tests should verify behavior, not implementation details.
